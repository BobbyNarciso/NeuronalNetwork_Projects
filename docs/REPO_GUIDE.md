# Repo Guide

- `homework/`: course assignments (each folder is self-contained)
- `extras/`: optional readings/experiments
- `assets/`: figures/results (safe to commit)

Tip: keep large datasets in a separate location (not committed) and document how to download them.
